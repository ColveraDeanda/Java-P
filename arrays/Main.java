package oracle_certification_preparation.arrays;

public class Main {

    // Formas de declarar un array.
    // Una vez creado un array. Como le asigno elementos.
    // Que pasa si asigno un elemento a un indice que no existe en el array.
    // cual es la sintaxis para crear y agregarle elementos a un array en una sola linea.
    // Puedo declarar un array y luego agregarle elementos en otra linea como se hizo en el ejemplo anterior ?
    // puedes explicarme y darme un ejemplo de length en un array es read-only ?
    // explica el metodo arrayCopy
    // Ejemplo de un array que esté guardando objetos.

    // -----------------------------------------------------
    // Dame un ejemplo de Polimorfismo, usando arrays.

    // -----------------------------------------------------
    // Dame ejemplos de arrays multidimensaionales, como agregarle data en una linea. Cuales es la sintaxys
    // Checar el video 131, viene info de arreglos multidimensionales.

    // ---------------------------------------------------------------------
    // Buscar metodos mas populares en java.util.Arrays para searching, sorting, filling and comparing.

    // Ejemplos de Arrays.sort()

    // Ejemplos de Arrays.binarySearch. ejemplo de un caso donde no encuentre un valor en el array.

    // Arrays.fill ejemplo de uso

    // ejemplos de usos de array donde puede causar errores.

    // Explicame el uso de varargs. Es el uso de los 3 puntitos.




}
