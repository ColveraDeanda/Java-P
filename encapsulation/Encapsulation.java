package oracle_certification_preparation.encapsulation;

public class Encapsulation {

    /**
     * La encapsulacion se refiere a proteger el acceso a los atributos de un objeto. Normalmente se ponen private.
     * Y el acceso y modificacion se hacen con getters y setters que seran public.
     *
     * Java tiene 4 modificadores de acceso:
     *  1. public: Se puede acceder a la clase desde cualquier lugar.
     *  2. private: Solo se puede usar dentro de la misma clase.
     *  3. protected: Se puede acceder dentro de la misma clase, o alguna otra clase del mismo paquete o una subclase.
     *  4. default: Se puede acceder dentro de la misma clase y cualquier clase dentro del mismo paquete.
     */



    // Quiz
    /**
     * How do you "hide your data" with encapsulation?
     * R = Modificar el estado del objeto a private y crear getters y setters para leer y modificar el estado.
     */

    /**
     * What are the benefits of "data hiding" from encapsulation?
     * R = Provee flexibilidad para modificar el estado del objeto y previene cambios invalidos de estado.
     */

    /**
     * True or False:  In order to provide encapsulation, every private instance variable needs to have a public getter and setter method.
     * R = No es estrictamente necesario que cada variable de instancia tenga su getter y setter. Es posible tener 1 o ninguno para cada variable
     * de instancia.
     */
}
